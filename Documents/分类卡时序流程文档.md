# 分类卡时序流程文档

**文档版本**: v1.0  
**创建日期**: 2026-03-23  
**适用项目**: Word Solitaire（词语联想接龙）

---

## 目录

1. [文档概述](#1-文档概述)
2. [分类卡定义](#2-分类卡定义)
3. [分类卡在分类槽的时序流程](#3-分类卡在分类槽的时序流程)
4. [分类卡在列区的时序流程](#4-分类卡在列区的时序流程)
5. [分类卡拖拽规则汇总](#5-分类卡拖拽规则汇总)
6. [代码实现说明](#6-代码实现说明)
7. [特殊场景说明](#7-特殊场景说明)
8. [与游戏流程的关系](#8-与游戏流程的关系)

---

## 1. 文档概述

### 1.1 文档目的

本文档详细说明分类卡在Word Solitaire游戏中的时序流程，包括分类卡的创建、放置、移动和完成判定等核心逻辑。

### 1.2 适用范围

- 开发人员：实现分类卡相关功能时参考
- 测试人员：验证分类卡逻辑是否符合设计
- 产品经理：了解分类卡的具体行为规则

### 1.3 相关文档

- [词语联想接龙-产品功能文档.md](./词语联想接龙-产品功能文档.md)
- [列区时序流程文档.md](./列区时序流程文档.md)

---

## 2. 分类卡定义

### 2.1 分类卡的特点

**分类卡**是表示词语类别的特殊卡牌，每个词组都有对应的分类卡（如"动物"、"水果"等）。

**核心属性**：
- `WordCardType`: `CategoryCard`（分类卡类型）
- `CategoryId`: 词组ID（如1=动物，2=水果等）
- `IsCategoryCard`: `true`（标识为分类卡）

**特点**：
- 每个词组有且只有一张分类卡
- 分类卡可以放在空白分类槽、空白列区、万能卡上
- 分类卡可以放在列区的同词组单词卡上
- 分类卡不能放在列区的分类卡上
- 分类卡放置到分类槽后不能再次移动

### 2.2 分类卡的创建

**代码位置**：`WordSolitaireCardLogic.cs` 第156-164行

```csharp
// 为每个类别创建分类卡
HashSet<int> categoryIds = new HashSet<int>();
foreach (WordItem word in _currentLevelWords)
{
    if (word != null)
    {
        categoryIds.Add(word.CategoryId);
    }
}

foreach (int categoryId in categoryIds)
{
    WordCategoryData category = WordDataManager.GetCategoryById(categoryId);
    if (category != null)
    {
        WordItem categoryItem = category.CreateCategoryCardItem();
        WordSolitaireCard categoryCard = CreateWordCard(categoryItem);
        if (categoryCard != null)
        {
            // categoryCard的WordCardType已通过InitWithWordItem设置为CategoryCard
            // IsCategoryCard属性会自动根据WordCardType计算，无需手动赋值
            allCards.Add(categoryCard);
        }
    }
}
```

**创建流程**：
1. 遍历当前关卡的所有单词卡
2. 收集所有CategoryId
3. 为每个CategoryId创建对应的分类卡
4. 将分类卡加入卡牌列表

---

## 3. 分类卡在分类槽的时序流程

### 3.1 空白分类槽状态

#### 3.1.1 操作时机

- 游戏开始时，所有分类槽都是空白状态
- 分类槽完成后，会变更为空白状态（但同一个分类槽只能完成一次）

#### 3.1.2 接受规则

**代码位置**：`WordSolitaireDeck.cs` 第103-112行

```csharp
case WordDeckType.CategorySlot:
    // 分类槽已满，不接受
    if (IsComplete)
        return false;
    
    // 分类槽为空时，只接受分类卡
    if (CurrentCardCount == 0)
    {
        return wordCard.IsCategoryCard && wordCard.CategoryId == CategoryId;
    }
```

**判断逻辑**：
```
判断条件：
1. 分类槽是否已满？ → 否
2. 分类槽是否为空？ → 是
3. 卡牌是否为分类卡？ → 是
4. 卡牌的CategoryId是否匹配分类槽的CategoryId？ → 是
结果：✅ 接受放置
```

#### 3.1.3 显示效果

根据产品文档第189-191行：

| 状态 | 显示内容 |
|------|---------|
| 有分类卡,无单词卡 | 显示分类卡完整信息:左上角为分类卡标识,右上角为`0/N`(N为该词组最大卡牌数量),中间为分类卡文本信息 |

**UI显示**：
- 左上角：分类卡标识
- 右上角：`0/N`（0表示当前已放置数量，N表示目标数量）
- 中间：分类卡文本信息（如"动物"）

#### 3.1.4 重要规则

- ✅ 分类卡放置后**不允许再次拖拽移动**
- ❌ 单词卡不能放在空白分类槽

**代码位置**：`WordSolitaireDeck.cs` 第386-390行

```csharp
case WordDeckType.CategorySlot:
case WordDeckType.Pack:
    // 分类槽和牌库中的牌不可拖动
    card.IsDraggable = false;
    break;
```

---

### 3.2 有分类卡状态

#### 3.2.1 操作时机

- 分类槽已放置分类卡，等待接收同词组的单词卡
- 玩家从列区、手牌区拖换单词卡到分类槽

#### 3.2.2 接受规则

**代码位置**：`WordSolitaireDeck.cs` 第114-119行

```csharp
// 分类槽已有分类卡后，只接受同词组的单词卡
// 万能卡不能放入分类槽
if (wordCard.IsJoker)
    return false;

return wordCard.CategoryId == CategoryId;
```

**判断逻辑**：
```
判断条件：
1. 分类槽是否有分类卡？ → 是（CurrentCardCount > 0）
2. 卡牌是否为万能卡？ → 否（拒绝）
3. 卡牌的CategoryId是否与分类卡相同？ → 是
结果：✅ 接受放置（单词卡）
```

#### 3.2.3 显示效果

根据产品文档第191行：

| 状态 | 显示内容 |
|------|---------|
| 有分类卡,有单词卡 | 显示最新放置的单词卡信息;单词卡左上角显示`Y/N`(Y为当前已放置数量,N为该词组最大数量),分类槽上方新增分类卡文本信息 |

**UI显示**：
- 主显示区：最新放置的单词卡信息
- 单词卡左上角：`Y/N`（Y为当前已放置数量）
- 上方：分类卡文本信息

#### 3.2.4 示例流程

**初始状态**：
```
分类槽：
[分类卡A-动物] CategoryId=1
右上角显示：0/5
```

**放置单词卡1**：
```
分类槽：
[分类卡A-动物]
[单词卡A1-猫] CategoryId=1
左上角显示：1/5
```

**放置单词卡2**：
```
分类槽：
[分类卡A-动物]
[单词卡A2-狗] CategoryId=1
[单词卡A3-猫] CategoryId=1
左上角显示：2/5
```

---

### 3.3 分类槽完成状态

#### 3.3.1 操作时机

- 当最后一张单词卡放入分类槽后
- 当前卡牌数量达到目标数量

#### 3.3.2 判断逻辑

**代码位置**：`WordSolitaireDeck.cs` 第60-61行

```csharp
public bool IsComplete => DeckType == WordDeckType.CategorySlot &&
                          CurrentCardCount >= TargetCardCount;
```

**判断条件**：
```
判断条件：
1. 牌堆类型是否为分类槽？ → 是
2. 当前卡牌数量 >= 目标卡牌数量？ → 是
结果：✅ 分类槽完成
```

#### 3.3.3 完成后的效果

根据产品文档第192行：

| 状态 | 显示内容 |
|------|---------|
| 分类卡对应单词卡数量已满 | 播放卡包收集动画,分类槽变更为空白状态,可以重新放置新的分类卡 |

**执行流程**：
1. ✅ 播放卡包收集动画
2. ✅ 分类槽变更为空白状态
3. ⚠️ 可以重新放置新的分类卡（但同一个分类槽只能完成一次）

**代码位置**：`WordSolitaireCardLogic.cs` 第465-485行

```csharp
private void OnCardMatchedToCategorySlot(WordSolitaireCard card, WordSolitaireDeck slot)
{
    // 发布匹配成功事件
    GameEventBus.PublishCategoryMatched(card.CategoryId, slot.CurrentCardCount, slot.TargetCardCount);
    
    // 检查分类槽是否完成
    if (slot.IsComplete)
    {
        // TODO: 添加分类完成事件
        // GameEventBus.PublishCategoryCompleted(card.CategoryId);
        
        // 播放完成音效
        if (AudioCtrl != null)
        {
            AudioCtrl.Play(AudioController.AudioType.MoveToAce);
        }
    }
    
    // 检查胜利条件
    CheckWinCondition();
}
```

---

### 3.4 分类槽拒绝场景汇总

| 场景 | 判断逻辑 | 结果 | 错误提示 |
|------|---------|------|---------|
| 单词卡放到空白分类槽 | `CurrentCardCount == 0 && !wordCard.IsCategoryCard` | ❌ 拒绝 | "只有分类卡才能放置在分类槽上" |
| 不同词组的分类卡 | `wordCard.CategoryId != CategoryId` | ❌ 拒绝 | 自动还原 |
| 不同词组的单词卡 | `wordCard.CategoryId != CategoryId` | ❌ 拒绝 | 自动还原 |
| 万能卡放到分类槽 | `wordCard.IsJoker` | ❌ 拒绝 | "万能卡不能放置在分类槽上" |
| 分类槽已满 | `IsComplete` | ❌ 拒绝 | 自动还原 |

---

## 4. 分类卡在列区的时序流程

### 4.1 空白列区状态

#### 4.1.1 接受规则

**代码位置**：`WordSolitaireDeck.cs` 第121-127行

```csharp
case WordDeckType.Column:
    // 列区：空白时接受分类卡或单词卡，有卡时按规则判断
    if (CurrentCardCount == 0)
    {
        // 空白列区接受分类卡或单词卡
        return wordCard.IsCategoryCard || !wordCard.IsJoker;
    }
```

**判断逻辑**：
```
判断条件：
1. 列区是否为空？ → 是
2. 卡牌是否为分类卡？ → 是
结果：✅ 接受放置
```

#### 4.1.2 示例

**初始状态**：
```
列区：空白
```

**放置分类卡**：
```
列区：
[分类卡A-动物] CategoryId=1
```

---

### 4.2 列区-万能卡状态

#### 4.2.1 接受规则

**代码位置**：`WordSolitaireDeck.cs` 第137-139行

```csharp
// 如果最后一张是万能卡，接受任何非万能卡
if (topCard.IsJoker)
    return !wordCard.IsJoker;
```

**判断逻辑**：
```
判断条件：
1. 列区顶部是否为万能卡？ → 是
2. 卡牌是否为万能卡？ → 否（拒绝）
3. 卡牌是否为分类卡？ → 是
结果：✅ 接受放置
```

#### 4.2.2 示例

**初始状态**：
```
列区：
[万能卡J] Joker
```

**放置分类卡**：
```
列区：
[万能卡J]
[分类卡A-动物] CategoryId=1
```

---

### 4.3 列区-单词卡状态

#### 4.3.1 接受规则

**代码位置**：`WordSolitaireDeck.cs` 第151-153行

```csharp
// 最后一张是单词卡
// 分类卡只能放在同词组的单词卡上
if (wordCard.IsCategoryCard)
    return topCard.CategoryId == wordCard.CategoryId;
```

**判断逻辑**：
```
判断条件：
1. 列区顶部是否为单词卡？ → 是
2. 卡牌是否为分类卡？ → 是
3. CategoryId是否相同？ → 是
结果：✅ 接受放置（分类卡放在单词卡上）
```

#### 4.3.2 示例

**初始状态**：
```
列区：
[单词卡A1-猫] CategoryId=1
```

**放置分类卡**：
```
列区：
[单词卡A1-猫] CategoryId=1
[分类卡A-动物] CategoryId=1
```

---

### 4.4 列区-分类卡状态

#### 4.4.1 接受规则

**代码位置**：`WordSolitaireDeck.cs` 第141-148行

```csharp
// 如果最后一张是分类卡
if (topCard.IsCategoryCard)
{
    // 分类卡不能放在分类卡上
    if (wordCard.IsCategoryCard) return false;
    // 单词卡不能放在分类卡上
    return false;
}
```

**判断逻辑**：
```
判断条件：
1. 列区顶部是否为分类卡？ → 是
2. 卡牌是否为分类卡？ → 否（拒绝）
3. 卡牌是否为单词卡？ → 是
结果：❌ 拒绝（单词卡不能放在分类卡上）
```

#### 4.4.2 示例

**初始状态**：
```
列区：
[分类卡A-动物] CategoryId=1
```

**尝试放置单词卡**：
```
操作：拖拽单词卡A2-狗（CategoryId=1）到列区
结果：❌ 拒绝
提示："单词卡不能放置在分类卡上，你需要将分类卡放在分类槽中"
```

---

### 4.5 列区拒绝场景汇总

| 场景 | 判断逻辑 | 结果 | 错误提示 |
|------|---------|------|---------|
| 分类卡放到分类卡上 | `topCard.IsCategoryCard && wordCard.IsCategoryCard` | ❌ 拒绝 | "此列以分类卡开始,你只能将卡槽上将卡牌堆叠在分类卡上" |
| 单词卡放到分类卡上 | `topCard.IsCategoryCard && !wordCard.IsCategoryCard` | ❌ 拒绝 | "单词卡不能放置在分类卡上，你需要将分类卡放在分类槽中" |
| 不同词组的分类卡 | `topCard.CategoryId != wordCard.CategoryId` | ❌ 拒绝 | "你只能堆叠同一词组的卡牌" |
| 不同词组的单词卡 | `topCard.CategoryId != wordCard.CategoryId` | ❌ 拒绝 | "你只能堆叠同一词组的卡牌" |

---

## 5. 分类卡拖拽规则汇总

### 5.1 可以拖拽到的位置

| 目标位置 | 条件 | 结果 |
|---------|------|------|
| 空白分类槽 | CategoryId匹配 | ✅ 接受 |
| 空白列区 | 无条件 | ✅ 接受 |
| 万能卡上 | 卡牌不是万能卡 | ✅ 接受 |
| 同词组的单词卡上 | CategoryId相同 | ✅ 接受 |

### 5.2 不能拖拽到的位置

| 目标位置 | 条件 | 结果 | 错误提示 |
|---------|------|------|---------|
| 已有分类卡的分类槽 | 分类槽已放置分类卡 | ❌ 拒绝 | 自动还原 |
| 不同词组的分类槽 | CategoryId不匹配 | ❌ 拒绝 | 自动还原 |
| 列区的分类卡上 | 列区顶部是分类卡 | ❌ 拒绝 | "单词卡不能放置在分类卡上，你需要将分类卡放在分类槽中" |
| 不同词组的单词卡上 | CategoryId不匹配 | ❌ 拒绝 | "你只能堆叠同一词组的卡牌" |

### 5.3 拖拽规则图

```
分类卡拖拽规则：

                    分类卡
                      ↓
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
   空白分类槽     空白列区      万能卡上
        │             │             │
    CategoryId    无条件      非万能卡
    匹配？         接受         接受
        │             │             │
        ├─是─→ ✅ 接受  ✅ 接受      ✅ 接受
        │             │
        └─否─→ ❌ 拒绝
                        ↓
                ┌───────┴───────┐
                ↓               ↓
           同词组单词卡      不同词组单词卡
                │               │
            CategoryId       CategoryId
            相同？           不同？
                │               │
                ├─是─→ ✅ 接受  ❌ 拒绝
                │
                └─否─→ ❌ 拒绝
```

---

## 6. 代码实现说明

### 6.1 CollectCardsToMove方法

**代码位置**：`WordSolitaireCardLogic.cs` 第412-449行

```csharp
/// <summary>
/// 收集需要一起移动的卡牌
/// 规则：拖拽的卡牌及其上方的同词组卡牌
/// </summary>
private List<WordSolitaireCard> CollectCardsToMove(WordSolitaireCard card, WordSolitaireDeck originalDeck)
{
    List<WordSolitaireCard> cardsToMove = new List<WordSolitaireCard>();
    cardsToMove.Add(card); // 添加主卡
    
    if (originalDeck == null)
    {
        return cardsToMove;
    }
    
    // 找到卡牌在牌堆中的索引
    int cardIndex = originalDeck.CardsArray.IndexOf(card);
    if (cardIndex < 0)
    {
        return cardsToMove;
    }
    
    // 从最后一张牌开始，向上收集同词组的卡牌
    for (int i = cardIndex - 1; i >= 0; i--)
    {
        WordSolitaireCard aboveCard = originalDeck.CardsArray[i] as WordSolitaireCard;
        if (aboveCard != null && aboveCard.CategoryId == card.CategoryId)
        {
            cardsToMove.Add(aboveCard);
        }
        else
        {
            // 遇到不同词组的卡牌，停止收集
            break;
        }
    }
    
    return cardsToMove;
}
```

**功能说明**：
1. 添加主卡（拖拽的卡牌）
2. 向上遍历，收集同词组的卡牌
3. 遇到不同词组的卡牌时停止收集

---

### 6.2 AcceptCard方法完整流程

**代码位置**：`WordSolitaireDeck.cs` 第96-170行

```csharp
public override bool AcceptCard(Card card)
{
    WordSolitaireCard wordCard = card as WordSolitaireCard;
    if (wordCard == null) return false;
    
    switch (DeckType)
    {
        case WordDeckType.CategorySlot:
            // 分类槽已满，不接受
            if (IsComplete)
                return false;
            
            // 分类槽为空时，只接受分类卡
            if (CurrentCardCount == 0)
            {
                return wordCard.IsCategoryCard && wordCard.CategoryId == CategoryId;
            }
            
            // 分类槽已有分类卡后，只接受同词组的单词卡
            // 万能卡不能放入分类槽
            if (wordCard.IsJoker)
                return false;
            
            return wordCard.CategoryId == CategoryId;
            
        case WordDeckType.Column:
            // 列区：空白时接受分类卡或单词卡，有卡时按规则判断
            if (CurrentCardCount == 0)
            {
                // 空白列区接受分类卡或单词卡
                return wordCard.IsCategoryCard || !wordCard.IsJoker;
            }
            else
            {
                WordSolitaireCard topCard = CardsArray[CurrentCardCount - 1] as WordSolitaireCard;
                if (topCard == null) return false;
                
                // 万能卡不能放入列区
                if (wordCard.IsJoker)
                    return false;
                
                // 如果最后一张是万能卡，接受任何非万能卡
                if (topCard.IsJoker)
                    return !wordCard.IsJoker;
                
                // 如果最后一张是分类卡
                if (topCard.IsCategoryCard)
                {
                    // 分类卡不能放在分类卡上
                    if (wordCard.IsCategoryCard) return false;
                    // 单词卡不能放在分类卡上
                    return false;
                }
                
                // 最后一张是单词卡
                // 分类卡只能放在同词组的单词卡上
                if (wordCard.IsCategoryCard)
                    return topCard.CategoryId == wordCard.CategoryId;
                
                // 单词卡只能放在同词组的单词卡上
                return topCard.CategoryId == wordCard.CategoryId;
            }
            
        case WordDeckType.Hand:
            // 手牌区：只接受来自牌库的卡牌
            return card.Deck != null && card.Deck.Type == Model.Enum.DeckType.DECK_TYPE_PACK;
            
        case WordDeckType.Pack:
            // 牌库：不接受外部卡牌
            return false;
            
        default:
            return false;
    }
}
```

---

## 7. 特殊场景说明

### 7.1 场景1：分类卡在列区底部一起拖拽到空白分类槽

#### 7.1.1 初始状态

```
列区：
索引0 → [单词卡A1-猫]  CategoryId=1
索引1 → [单词卡A2-狗]  CategoryId=1
索引2 → [分类卡A-动物] CategoryId=1 ← 最后一张（最下方）

分类槽（空白）
```

#### 7.1.2 操作流程

**步骤1**：玩家拖拽分类卡A
```
CollectCardsToMove 收集同词组卡牌：
cardsToMove = [
    [分类卡A-动物]  ← 主卡（索引2）
    [单词卡A2-狗]   ← 同词组（索引1）
    [单词卡A1-猫]   ← 同词组（索引0）
]
```

**步骤2**：拖拽到空白分类槽
```
AcceptCard判断（分类卡A）：
- CurrentCardCount == 0? ✅ 是
- IsCategoryCard? ✅ 是
- CategoryId == CategoryId? ✅ 是
结果：✅ 接受
```

**步骤3**：PlaceCardToDeck执行放置
```
第1次 PushCard(分类卡A) → CurrentCardCount = 0 → ✅ 放入
第2次 PushCard(单词卡A2) → CurrentCardCount = 1 → ✅ 放入
第3次 PushCard(单词卡A1) → CurrentCardCount = 2 → ✅ 放入
```

#### 7.1.3 最终状态

```
列区：空

分类槽：
[分类卡A-动物]  ← 第一张放置
[单词卡A2-狗]   ← 第二张放置
[单词卡A1-猫]   ← 第三张放置

分类槽显示：
- 显示最新放置的单词卡A1的信息
- 左上角显示 3/N（假设N=5）
- 上方显示分类卡A的文本信息
```

#### 7.1.4 关键点

- ✅ 分类卡在列区底部可以拖拽
- ✅ 自动收集上方的同词组单词卡
- ✅ 一起拖拽到空白分类槽
- ✅ 第一张放入分类卡，后续放入单词卡
- ✅ 所有卡牌都能成功放置

---

### 7.2 场景2：分类卡放置后的不可移动性

#### 7.2.1 操作流程

**步骤1**：将分类卡A放置到分类槽
```
分类槽：
[分类卡A-动物] CategoryId=1
```

**步骤2**：尝试拖拽分类卡A
```
代码检查：
case WordDeckType.CategorySlot:
    card.IsDraggable = false;

结果：❌ 无法拖拽
```

#### 7.2.2 原因说明

- 产品规则：分类卡放置到分类槽后不允许再次拖拽移动
- 代码实现：分类槽中的卡牌IsDraggable设置为false
- 设计意图：分类卡作为基础标识，固定在分类槽中

---

### 7.3 场景3：分类槽完成后变更为空白

#### 7.3.1 初始状态

```
分类槽：
[分类卡A-动物]
[单词卡A1-猫]
[单词卡A2-狗]
[单词卡A3-兔]
[单词卡A4-鸟]
[单词卡A5-鱼]  ← 达到目标数量（假设N=5）

左上角显示：5/5
```

#### 7.3.2 判断逻辑

```csharp
public bool IsComplete => DeckType == WordDeckType.CategorySlot &&
                          CurrentCardCount >= TargetCardCount;

判断：
- DeckType == WordDeckType.CategorySlot? ✅ 是
- CurrentCardCount (5) >= TargetCardCount (5)? ✅ 是
结果：✅ 分类槽完成
```

#### 7.3.3 完成后的效果

```
执行流程：
1. 播放卡包收集动画
2. 分类槽变更为空白状态
3. 可以重新放置新的分类卡（但同一个分类槽只能完成一次）

最终状态：
分类槽：空白
```

---

## 8. 与游戏流程的关系

### 8.1 游戏开始阶段

```
游戏开始
    ↓
初始化关卡
    ↓
创建所有单词卡
    ↓
为每个词组创建分类卡
    ↓
洗牌
    ↓
分发卡牌到列区
    ↓
剩余卡牌放入牌库
    ↓
所有分类槽处于空白状态
```

---

### 8.2 放置分类卡阶段

```
玩家从列区或手牌区拖拽分类卡
    ↓
[判断] 分类槽是否为空？
    ├─ 否 → ❌ 拒绝
    └─ 是 → 继续
            ↓
        [判断] CategoryId是否匹配？
            ├─ 否 → ❌ 拒绝
            └─ 是 → ✅ 接受放置
                    ↓
                分类卡固定在分类槽
```

---

### 8.3 收集单词卡阶段

```
分类槽已有分类卡
    ↓
玩家拖拽同词组的单词卡到分类槽
    ↓
[判断] CategoryId是否匹配？
    ├─ 否 → ❌ 拒绝
    └─ 是 → ✅ 接受放置
            ↓
        更新显示数量 Y/N
            ↓
        [判断] 是否达到目标数量？
            ├─ 否 → 继续等待单词卡
            └─ 是 → ✅ 分类槽完成
```

---

### 8.4 通关判定阶段

```
检查所有分类槽
    ↓
[判断] 所有分类槽是否完成？
    ├─ 否 → 继续游戏
    └─ 是 → ✅ 通关
            ↓
        弹出通关奖励界面
```

**代码位置**：`WordSolitaireCardLogic.cs` 第511-527行

```csharp
public bool CheckWinCondition()
{
    if (CategorySlots == null || CategorySlots.Length == 0)
        return false;
    
    // 所有分类槽都必须完成
    foreach (var slot in CategorySlots)
    {
        if (slot == null) continue;
        if (!slot.IsComplete)
            return false;
    }
    
    // 触发胜利
    OnWinGame();
    return true;
}
```

---

### 8.5 完整游戏流程图

```
Word Solitaire 完整游戏流程：

游戏开始
    ↓
初始化关卡 → 创建单词卡和分类卡 → 洗牌 → 分发卡牌
    ↓
所有分类槽空白，等待分类卡
    ↓
玩家放置分类卡到分类槽
    ↓
分类卡固定，等待单词卡
    ↓
玩家逐个放置同词组单词卡到分类槽
    ↓
[循环] 检查数量是否达到目标？
    ├─ 否 → 继续放置单词卡
    └─ 是 → 分类槽完成
            ↓
        播放收集动画 → 分类槽变更为空白
            ↓
        [判断] 所有分类槽是否完成？
            ├─ 否 → 继续放置其他分类卡
            └─ 是 → ✅ 通关
                    ↓
                弹出通关奖励界面
```

---

## 附录

### 附录A：关键代码文件

| 文件路径 | 功能描述 |
|---------|---------|
| `Assets/SimpleSolitaire/Resources/Scripts/Controller/WordSolitaire/WordSolitaireDeck.cs` | 牌堆管理，包含AcceptCard方法 |
| `Assets/SimpleSolitaire/Resources/Scripts/Controller/WordSolitaire/WordSolitaireCardLogic.cs` | 游戏逻辑，包含CollectCardsToMove和PlaceCardToDeck方法 |
| `Assets/SimpleSolitaire/Resources/Scripts/Controller/WordSolitaire/WordSolitaireCard.cs` | 卡牌类，定义分类卡属性 |
| `Assets/SimpleSolitaire/Resources/Scripts/Controller/Base/Card.cs` | 卡牌基类，处理拖拽事件 |

---

### 附录B：关键数据结构

```csharp
// WordDeckType 枚举
public enum WordDeckType
{
    Pack,          // 牌库
    Hand,          // 手牌区
    Column,        // 列区
    CategorySlot   // 分类槽
}

// WordSolitaireCard 属性
public class WordSolitaireCard : Card
{
    public CardType WordCardType { get; set; }  // CategoryCard, WordCard, Joker
    public int CategoryId { get; set; }         // 词组ID
    public bool IsCategoryCard => WordCardType == CardType.CategoryCard;  // 是否为分类卡
    public bool IsJoker => WordCardType == CardType.Joker;               // 是否为万能卡
}
```

---

### 附录C：错误提示汇总

| 场景 | 提示文本 |
|------|---------|
| 单词卡放到空白分类槽 | **只有分类卡才能放置在分类槽上** |
| 单词卡放到列区-分类卡 | **单词卡不能放置在分类卡上，你需要将分类卡放在分类槽中** |
| 分类卡放到不同词组的单词卡 | **你只能堆叠同一词组的卡牌** |
| 单词卡放到不同词组的单词卡 | **你只能堆叠同一词组的卡牌** |
| 分类卡放到列区-分类卡 | **此列以分类卡开始,你只能将卡槽上将卡牌堆叠在分类卡上** |
| 万能卡放到分类槽 | **万能卡不能放置在分类槽上** |

---

## 文档更新记录

### v1.0 (2026-03-23)

**初始版本**

**主要内容**：
- 完整描述分类卡的定义和特点
- 详细说明分类卡在分类槽的时序流程
- 详细说明分类卡在列区的时序流程
- 汇总分类卡拖拽规则
- 提供代码实现说明和特殊场景示例
- 说明分类卡与游戏流程的关系

**参考文档**：
- 词语联想接龙-产品功能文档.md
- 列区时序流程文档.md
- WordSolitaireCardLogic.cs
- WordSolitaireDeck.cs

---

*文档结束*
